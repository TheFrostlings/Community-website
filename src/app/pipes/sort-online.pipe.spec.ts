import { SortOnlinePipe } from './sort-online.pipe';

describe('SortOnlinePipe', () => {
  it('create an instance', () => {
    const pipe = new SortOnlinePipe();
    expect(pipe).toBeTruthy();
  });
});
